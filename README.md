# Radar-system
Arduino Radar System is a sensor-based project that uses an ultrasonic sensor and servo motor to scan the surroundings. It detects nearby objects by measuring distance and displays radar-like tracking data through a computer interface. When an object comes within a specific range, it triggers an alert using a buzzer.
